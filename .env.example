VITE_ASTROSHARE_API_URL=https://dev-api.astroshare.fr

# Optionnel : active la vérification du relief (horizon obstrué par une colline/montagne) dans la
# note de conditions de visibilité. Sans token, l'app se rabat sur la note basée sur l'altitude seule.
# Token public Mapbox (gratuit) : https://account.mapbox.com/access-tokens/
VITE_MAPBOX_TOKEN=
